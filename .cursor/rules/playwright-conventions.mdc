---
description: How this project writes Playwright tests — auth, POM, locators, data and cleanup
globs: tests/**
alwaysApply: false
---

# Playwright Conventions

Full standard for specs under `tests/**`. Prefer patterns already in this repo; where a section has no in-repo example yet, the ✅ block is **advised**.

## Refusals (hard stops)

The agent must **refuse** these requests — stop, explain the conflict, and do not apply a workaround:

| Refuse | Conflict |
|--------|----------|
| Weakening, deleting, or skipping an assertion so a test passes | Masks real failure; green via weakened asserts is a defect, not a fix |
| Self-healing or patching tests when triage classified the failure as a **real app bug** | Heal is for locator drift only; route to bug-reporter or human confirmation |
| Replacing a role-based locator with CSS or XPath | Violates locator standard; fix the accessible name/scope, not the selector style |
| Adding or keeping `waitForTimeout` | Fixed sleeps cause flake; use web-first `expect` on locators |
| Hardcoding credentials, tokens, or API keys in specs, POMs, or config | Secrets belong in env/secrets; hardcoded values leak and break environments |
| Using TypeScript `any` (or untyped escape hatches) in test or POM code | Erases compile-time safety; use proper types or narrow unknowns |

On refusal: state which rule blocks the action, cite triage/classification or the convention section if relevant, and propose the allowed path (e.g. POM locator fix, Jira bug, env var).

## 1. Locators

**Standard:** Role/label first; `getByTestId` escape hatch only (comment why); never CSS/XPath; disambiguate with `.filter({ hasText })`, not `.first()`.

```typescript
// ✅ Good — from pages/ProgramsPage.ts
this.newProgramButton = page.getByRole("button", { name: "+ New Program" });
return this.programsTable
  .getByRole("row")
  .filter({ has: this.page.getByText(name, { exact: true }) });
```

```typescript
// ❌ Bad
page.locator(".btn-primary");
page.locator("//button[1]");
page.getByRole("button", { name: /^Edit / }).first();
```

**Why:** Prevents brittle DOM coupling and non-deterministic matches when multiple similar controls exist.

## 2. Waiting

**Standard:** Web-first assertions only; never `waitForTimeout`; never snapshot booleans from `isVisible()`.

```typescript
// ✅ Good — from tests/ds1-create-program.spec.ts
await expect(programsPage.heading).toBeVisible();
await expect(modal.dialog).toBeVisible();
```

```typescript
// ❌ Bad
await page.waitForTimeout(2000);
expect(await modal.dialog.isVisible()).toBe(true);
```

**Why:** Prevents race flake from fixed sleeps and one-shot visibility checks that do not retry.

## 3. Page Objects

**Standard:** One POM per page/component; locators in the constructor; no assertions in POMs; specs never inline locators.

```typescript
// ✅ Good — from pages/NewProgramModal.ts + ds1 spec
async fillProgramName(name: string): Promise<void> {
  await this.programName.fill(name);
}
// in spec:
await programsPage.openNewProgramForm();
await expect(modal.dialog).toBeVisible();
```

```typescript
// ❌ Bad
async openForm(): Promise<void> {
  await this.newProgramButton.click();
  await expect(this.dialog).toBeVisible(); // assertion in POM
}
// in spec:
await page.getByRole("button", { name: "+ New Program" }).click();
```

**Why:** Prevents duplicated locators and mixed intent/mechanics that break every screen when the UI shifts.

## 4. Auth

**Standard:** `storageState` via the setup project for feature tests; empty session + UI login only for login-page tests; API login for speed / programmatic setup.

```typescript
// ✅ Good — playwright.config.ts setup + storageState dependency
{ name: "setup", testMatch: /.*\.setup\.ts/ },
{
  name: "chromium",
  use: { ...devices["Desktop Chrome"], storageState: "playwright/.auth/user.json" },
  dependencies: ["setup"],
}
```

```typescript
// ❌ Bad — logging in via UI at the start of every feature test
test.beforeEach(async ({ page }) => {
  await page.goto("/login");
  await page.getByLabel("Email").fill(process.env.DIDAXIS_EMAIL!);
  await page.getByRole("button", { name: "Sign In" }).click();
});
```

**Why:** Prevents slow, flaky per-test login and session drift across the suite. See `auth-strategy.md`.

## 5. Test data

**Standard:** Unique records + self-cleanup (`uniqueName()` / `trackProgram`). Happy-path structured data from Faker factories under `test-data/factories/`. Curated invalid inputs in static `.ts` files (not Faker). Secrets and URLs from `process.env`. Repeated paths and UI/API messages from enums.

```typescript
// ✅ Good — factory for happy-path data (test-data/factories/program.ts)
import { programFactory } from "../test-data/factories/program";

const program = programFactory();
await programsPage.createProgram(program.name, trackProgram, program.description);
```

```typescript
// ❌ Bad
const programName = "Web Development 2026"; // shared, collisions
await page.goto("https://test.didaxis.studio/programs"); // hardcoded URL
await expect(page.getByText("Program created successfully!!!")).toBeVisible(); // invented copy
```

**Factories (happy path):** `test-data/factories/*.ts`. Example:

```typescript
import { faker } from "@faker-js/faker";

export type ProgramInput = {
  name: string;
  description: string;
};

export function programFactory(overrides: Partial<ProgramInput> = {}): ProgramInput {
  return {
    name: `${faker.commerce.department()} ${Date.now()}`,
    description: faker.lorem.sentence(),
    ...overrides,
  };
}
```

**Invalid sets:** static files such as `test-data/invalid/program-names.ts` — empty, whitespace, over-max length. Do not generate invalid data with Faker.

**Env:** `process.env.DIDAXIS_URL`, `DIDAXIS_EMAIL`, `DIDAXIS_PASSWORD`, `DIDAXIS_API_TOKEN` (never literals).

**Enums:** when the same path or message appears in 2+ places (e.g. `Routes.Programs = "/programs"`).

Creating a program and deleting it via `trackProgram` is **not** shared-state mutation — keep the importance tag (`@smoke` / `@sanity` / `@regression` / `@api` / `@e2e`). Do not add a manual `afterAll` for that; the cleanup fixture handles it.

**Why:** Prevents collisions, leaked secrets, invented copy, and polluted Didaxis data.

## 6. API testing

**Standard:** Prefer Playwright `request` for setup, teardown helpers, and contract checks; never hardcode tokens; never delete data the test did not create. *(Program teardown today uses `fixtures/cleanup.fixture.ts` — keep that path for UI-created programs.)*

```typescript
// ✅ Good — advised (no `request`-based contract check in repo yet)
test("GET /api/programs returns an array", async ({ request }) => {
  const res = await request.get("/api/programs", {
    headers: { Authorization: `Bearer ${process.env.DIDAXIS_API_TOKEN}` },
  });
  expect(res.ok()).toBeTruthy();
  expect(Array.isArray(await res.json())).toBeTruthy();
});
```

```typescript
// ❌ Bad
const res = await fetch("https://hardcoded-host/api/programs", {
  headers: { Authorization: "Bearer eyJhbGciOi..." }, // hardcoded token
});
```

**Why:** Prevents environment lock-in, leaked secrets, and teardown that destroys shared or foreign data.

## 7. Network mocking

**Standard:** `page.route` / `fulfill` (or abort) for **500** / **503** / **timeout** / empty / malformed; never mock the endpoint under test; observe real UI copy first; exactly one tag per test (`@api` for mock/contract cases).

```typescript
// ✅ Good — advised shape (repo has abort-style TC-012; prefer fulfill + narrow URL)
await page.route("**/api/programs**", async (route) => {
  if (route.request().method() === "POST") {
    return route.fulfill({ status: 503, body: '{"error":"unavailable"}' });
  }
  return route.continue();
});
```

```typescript
// ❌ Bad
await page.route("**/*", (route) => route.abort()); // too broad; blinds the whole app
// inventing toast text the app never shows
await expect(page.getByText("Service Unavailable!!!")).toBeVisible();
```

**Why:** Prevents non-deterministic live-API edge flakes and false failures from invented copy or over-broad routes. See `.cursor/skills/network-mocked-edge-cases/`.

## 8. Accessibility

**Standard:** Axe with `.withTags(['wcag2a','wcag2aa'])` on key pages + a keyboard-nav check; never `.disableRules()` to go green.

```typescript
// ✅ Good — advised (programs.a11y.spec.ts exists but lacks withTags / keyboard / still uses disableRules)
const results = await new AxeBuilder({ page })
  .withTags(["wcag2a", "wcag2aa"])
  .analyze();
await expect(results.violations).toEqual([]);

await programsPage.newProgramButton.focus();
await expect(programsPage.newProgramButton).toBeFocused();
await page.keyboard.press("Enter");
await expect(programsPage.newProgramModal.dialog).toBeVisible();
```

```typescript
// ❌ Bad — from current modal a11y pattern to avoid going forward
const results = await new AxeBuilder({ page })
  .disableRules(["color-contrast"]) // silences real issues
  .analyze();
```

**Why:** Prevents shipping inaccessible UI and false-green a11y suites. See `.cursor/skills/a11y-checks/`.

## 9. Assertions

**Standard:** Web-first expects; `expect.soft` for independent multi-checks; visual (`toHaveScreenshot`) only where it earns it.

```typescript
// ✅ Good — soft multi-check advised (repo uses hard expects in ds1 TC-001 today)
await expect.soft(modal.programName).toBeVisible();
await expect.soft(modal.description).toBeVisible();
await expect.soft(modal.createButton).toBeDisabled();
// Visual only when intentional:
// await expect(programsPage.heading).toHaveScreenshot("programs-heading.png");
```

```typescript
// ❌ Bad
await expect(modal.programName).toBeVisible();
await expect(modal.description).toBeVisible(); // hard stop hides later failures
await expect(page).toHaveScreenshot(); // blanket visual by default
```

**Why:** Prevents masked secondary failures and brittle pixel diffs on layouts that are not under visual contract.

## 10. Anti-flake

**Standard:** CI-only retries (max 2); isolation; `page.clock` for relative time; pinned `locale` / `timezoneId`; never `workers: 1` except `@destructive` (`npm run test:destructive`).

```typescript
// ✅ Good — config retries already in playwright.config.ts; clock advised (no in-repo clock yet)
// retries: process.env.CI ? 2 : 0
await page.clock.install({ time: new Date("2026-08-04T18:00:00Z") });
```

```typescript
// ❌ Bad
// retries: 5
// workers: 1  // only allowed for npm run test:destructive
const label = `Updated ${new Date().toLocaleString()}`; // wall-clock drift local vs CI
let sharedProgramName = ""; // mutable state across tests
```

**Why:** Prevents hidden flake, serial bottlenecks, and date/time assertions that pass locally and fail in CI. *(Pin `locale` + `timezoneId` in config once chosen — not set yet.)*

## 11. Tags

**Standard:** Exactly **one** tag per `test()` — never on `describe`. Allowed values only:

| Tag | When |
|-----|------|
| `@smoke` | Critical path that must pass to ship |
| `@sanity` | Fast confidence after a build or deploy |
| `@regression` | Full functional coverage |
| `@api` | Contract checks, `request` helpers, or network-mocked UI |
| `@e2e` | Cross-page user journey |
| `@destructive` | Mutates **shared/global** state (locale, roles, flags, settings) |

`@destructive` is **only** for shared/global mutation and **must run serially** (`npm run test:destructive` → `--workers=1`). A test that creates records and cleans up **only its own data** (`trackProgram`) keeps its importance tag — do **not** retag it `@destructive`.

Any test that mutates shared state **must** revert it in `afterEach` or `afterAll` (restore locale/role/flag/setting). Missing revert is a defect.

```typescript
// ✅ Good
test.describe("org locale", () => {
  let originalLocale: string;

  test.beforeEach(async ({ page }) => {
    originalLocale = await settingsPage.readLocale();
  });

  test.afterEach(async ({ page }) => {
    await settingsPage.setLocale(originalLocale);
  });

  test("TC-001 — switching locale updates labels", { tag: "@destructive" }, async ({
    page,
  }) => {
    await settingsPage.setLocale("fr");
    await expect(programsPage.heading).toHaveText(/programmes/i);
  });
});
```

```typescript
// ❌ Bad
test.describe("programs @regression", () => { /* tag on describe */ });
test("creates a program", { tag: ["@smoke", "@e2e"] }, async () => { /* two tags */ });
test("creates a program", { tag: "@destructive" }, async () => {
  // own program + trackProgram — this is not shared-state mutation
});
test("changes locale", { tag: "@destructive" }, async () => {
  await settingsPage.setLocale("fr");
  // no afterEach revert — leaves the env dirty
});
```

**Why:** Precise CI filtering (`npm run test:smoke`, …) without parallel tests clobbering org-wide settings.

---

## Do / Don't

| Do | Don't |
|----|--------|
| Use `getByRole` / `getByLabel` / `getByText` | Use CSS, XPath, or brittle full-page text |
| Wait with `expect(locator).toBeVisible()` | Use `waitForTimeout` or `expect(await isVisible())` |
| Put locators and actions in POMs | Inline locators or `expect` inside POMs |
| Rely on setup `storageState` | UI-login before every feature test |
| `uniqueName()` / factories + `trackProgram` | Fixed names or leave programs behind |
| Faker factories; static invalid sets; env; enums | Hardcoded URLs, secrets, or invented copy |
| One tag: `@smoke` / `@sanity` / `@regression` / `@api` / `@e2e` / `@destructive` | Tags on `describe`; multiple tags; `@destructive` for self-cleaned data |
| Revert shared state in `afterEach` / `afterAll` | Leave locale, roles, flags, or settings changed |
| Prefer `request` for API/contract checks | Hardcode tokens or delete foreign data |
| Narrow `page.route` + observed UI copy | Mock the endpoint under test or invent strings |
| Axe `wcag2a`/`wcag2aa` + keyboard check | `.disableRules()` to go green |
| `expect.soft` for multi-checks; visual sparingly | Hard-stop every check; screenshot everything |
| CI retries ≤2, isolate, `page.clock`, pin locale/TZ | Raise retries; `workers: 1` except `@destructive` |